apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexuscart.fullname" . }}
  labels:
    {{- include "nexuscart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nexuscart.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
