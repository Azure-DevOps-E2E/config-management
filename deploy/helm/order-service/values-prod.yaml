# Order data is in-memory; keep one replica until shared persistence is added.
replicaCount: 1

resources:
  requests:
    cpu: 300m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi

# BEGIN AZURE PIPELINES MANAGED IMAGE
image:
  repository: devopse2e.azurecr.io/order-service
  tag: "v1.0.134"
appVersion: "v1.0.134"
# END AZURE PIPELINES MANAGED IMAGE
