parameters:
- name: workingDirectory
  type: string
  default: '$(Build.SourcesDirectory)'

steps:
- bash: |
    set -euo pipefail
    go vet ./...
    go build ./...
  displayName: 'Vet and build Go packages'
  workingDirectory: ${{ parameters.workingDirectory }}
